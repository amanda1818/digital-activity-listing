fastapi>=0.110
uvicorn[standard]>=0.29
sqlalchemy>=2.0
pydantic>=2.6
pandas>=2.2
numpy>=1.26
streamlit>=1.33
python-pptx>=0.6.23
anthropic>=0.25
python-dotenv>=1.0
cryptography>=42.0
APScheduler>=3.10
scikit-learn>=1.4
joblib>=1.3
requests>=2.31

# Optional — only needed when implementing the live connectors (untested in sandbox):
# msal>=1.28
# google-api-python-client>=2.120
# google-auth>=2.29
# Optional — desktop agent on the respective OS:
# psutil>=5.9
# pywin32>=306                  ; Windows only
# pyobjc-framework-Quartz>=10   ; macOS only
